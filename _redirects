# Netlify Redirects - FreecalcHub
# MINIMAL: Only for external legacy URLs and canonical domain enforcement

# ========================================
# FORCE HTTPS AND WWW (Required for SEO)
# ========================================
http://freecalchub.com/* https://www.freecalchub.com/:splat 301
http://www.freecalchub.com/* https://www.freecalchub.com/:splat 301
https://freecalchub.com/* https://www.freecalchub.com/:splat 301

# ========================================
# LEGACY EXTERNAL URLS (Google indexed)
# ========================================
# These are old URLs Google indexed that we can't fix at source
# Internal links should be fixed in HTML files instead

# Old path restructuring (Google indexed these)
/finance/business/* /business/:splat 301
/date-time/date-difference/days-between/* /date-time/date-difference/:splat 301
/date-time/age/age-calculator/* /date-time/age/:splat 301

# Policy page consolidation (old URLs Google indexed)
/policy/ /privacy/ 301
/privacy-policy/ /privacy/ 301
/terms-of-service/ /terms/ 301
/cookies/ /privacy/ 301

# Old blog paths (Google indexed)
/blog/articles/understanding-compound-interest/ /blog/articles/finance/understanding-compound-interest/ 301

# ========================================
# CUSTOM ERROR PAGE (MUST BE LAST)
# ========================================
/* /404.html 404
